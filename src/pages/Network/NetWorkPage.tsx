import HeaderTitle from '@/components/common/HeaderTitle.tsx';

export default function NetWorkPage() {
  return (
    <>
      <HeaderTitle title={'NetWork'} description={'IP 조회, 변경, Mac 주소 조회 등 '} />
    </>
  );
}
